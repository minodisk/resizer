#!/bin/bash

docker-compose stop
docker-compose rm -f
docker-compose build

docker-compose \
  -f docker-compose.yml \
  -f docker-compose.run.yml \
  up -d mysql
sleep 10
docker-compose \
  -f docker-compose.yml \
  -f docker-compose.run.yml \
  up app
